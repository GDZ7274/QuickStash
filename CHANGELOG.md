# Changelog

本文件记录 QuickStash 已确认的用户可见变化。早期未形成可靠记录的版本不做推测性补写。

## 1.1.0 (4) - 2026-08-31

### 改进

- 重复复制已有文字、链接或图片时刷新原记录到最新位置，不再堆积重复历史；已经固定的记录继续保持固定。
- 图片去重改为比较统一解码后的像素指纹；解码后像素完全一致时，不受 PNG、TIFF、JPEG、HEIC/HEIF、WebP 容器或编码字节差异影响。
- 所有新剪贴板图片继续统一规范化为真实 PNG，保留 EXIF 方向和 Alpha，并强化 provider 超时、迟到结果、保存重试和退出排空的稳定性。
- 启动 bootstrap 合并重复项时记录 incoming ID 到 canonical ID 的别名，期间发生的删除、固定、复制确认和迟到图片复制会继续命中最终保留的记录。
- 冗余图片丢弃在移动前持久化删除 intent；新版 intent 记录设备号与 inode，恢复前核对身份，避免误删同路径后来出现的新文件。失败或崩溃后由启动恢复继续完成，不会因一次清理失败永久遗留孤儿文件。
- 损坏 canonical 图片的 backing 替换改为可恢复的两阶段流程：先写 replacement intent，再切换并刷入 metadata，随后隔离旧 backing 并确认 manifest。
- 启动恢复会自动迁移旧版同像素图片记录，保留 canonical ID、固定状态并采用最新复制时间；损坏 canonical 也可在同指纹有效 orphan 存在时接管其 backing。
- App 退出采用 8 秒全局总预算，完成或超时均只回复 macOS 一次；主线程不等待文件 I/O 或 semaphore，未完成的可恢复清理由下次启动继续。
- 设置页新增 `Design by GDZ` 标志和从 App bundle 动态读取的版本号。

### 源码发布

- 仓库调整为公开可查看源码，新增保留全部权利的 source-available 许可和可核验的安全报告流程；公开可见不代表授予开源、再分发或商用许可。

## 1.1.0 (3) - 2026-08-27

### 修复

- 恢复卡片右上角复制/打开按钮旁的内联删除按钮，并让卡片拖拽命中区避开两个操作按钮。
- 剪贴板 TIFF、JPEG、HEIC/HEIF 和 WebP 等图片统一转换为真实 PNG 落盘，不再按来源格式保存 TIFF。
- PNG provider 不可用、暂不完整或输出超限时继续尝试 TIFF/JPEG/HEIC/WebP 等后备表示。
- 修复同步图片 provider 超过 2 秒后返回时被丢弃、旧请求误提交新 `changeCount`、以及读取 lane 可能无界堆积的问题。
- 图片保存瞬时失败会有限重试；关闭记录或清空历史时会立即停止等待中的下一次重试。
- 旧 TIFF 历史复制到系统剪贴板时在内存中转为 PNG，保留旧 metadata、orphan 恢复和删除兼容性。
- 图片 provider 永久不给数据时保留并最终提交同一复制事件中的文字或链接后备，不再整条丢失。
- 纯文字和 URL 在一次稳定读取后立即进入历史，不再重复启动图片确认 helper；修复由确认延迟造成的文字、链接漏记或顺序滞后。

### 安全与验证

- 图片转码增加 PNG chunk/CRC 校验、立即像素解码、EXIF 方向、Alpha、单边尺寸、位深/通道解码内存及输出大小限制。
- 新增 JPEG、HEIC、WebP、TIFF 回退、旧 TIFF、迟到 provider、两 lane 上限、有界退出和保存取消测试。

## 1.1.0 (2) - 2026-07-28

### 新增

- 全局快捷键、状态栏菜单和主窗口相机按钮三个截图入口。
- 默认 `Command-Shift-A` 的可配置 Carbon 非独占快捷键。
- ScreenCaptureKit 多屏独立捕获和物理像素选区。
- 8 个选区手柄、窗口单击吸附和可调圆角。
- 箭头、无填充矩形、自由涂鸦、马赛克和文字标注。
- 已有标注选择、移动、调整、改色、删除和 100 步撤销/重做。
- 中文输入法文本编辑。
- PNG 剪贴板复制和 PNG/JPEG 另存为，JPEG 质量 `0.95`。
- 普通文字、HTTP(S) 链接、PNG 和 TIFF 的实时剪贴板历史。
- QuickStash 截图复制后的本地历史同步。
- 正常退出时最后一条文字、链接和进行中图片的持久化排空。

### 修复

- 修复普通窗口靠近菜单栏图标时误弹收录窗口的问题。
- 修复菜单栏拖放高频事件导致 AppKit 拖拽事务卡住的问题。
- 使用显式源路径的 `FailSelectedQuarantine` 替代依赖 `Set<UUID>` 顺序的失败注入。
- 删除 `phase3:` 临时诊断输出。
- 修复迟到捕获、渲染、编码或保存结果污染新截图会话的风险。
- 修复输出锁跨越文件 I/O 造成主线程间接等待的风险。
- 修复退出时最后一条 provisional 文字、链接或进行中图片可能丢失的问题。
- 禁止 Release 构建携带代码覆盖率插桩，避免正式 App 生成 `default.profraw` 测试残留。
- 消除过期剪贴板图片恢复测试在文件隔离与主线程元数据更新之间的竞态。

### 保留与强化

- 文件复制进度、取消、配额和磁盘空间预检。
- 原子导入暂存、持久化恢复、orphan 恢复和七天 Trash。
- 剪贴板记录授权、保留数量和保留期限。
- 截图期间暂停菜单栏拖文件呈现，结束后恢复。
