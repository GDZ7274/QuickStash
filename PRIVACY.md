# QuickStash 隐私说明

更新时间：2026-08-31

## 原则

QuickStash 当前版本是本地优先的 macOS 工具。源码中没有网络请求、遥测、广告、账号登录、远程分析或云同步逻辑。除非未来版本明确增加并在文档中披露，否则应用不会主动把文件、截图或剪贴板内容发送到本机之外。

## 剪贴板记录

- 首次使用时必须在应用内明确选择“启用实时记录”。
- 未授权或关闭记录后，QuickStash 不会读取新的系统剪贴板内容。
- 启用后可记录普通文字、HTTP(S) 链接，以及 PNG、TIFF、JPEG、HEIC/HEIF、WebP 等 macOS 可解码图片。
- 新记录的外部图片只在本机使用 ImageIO 转换，统一以真实 PNG 保存；旧 TIFF 历史不会被后台批量改写，复制时才在内存中转换为 PNG。
- 图片源、像素尺寸、估算解码内存和 PNG 输出均有上限；暂未完整提供的数据只做有限重试。
- QuickStash 自身复制的截图会在系统剪贴板写入成功后记录为本地图片历史，并保证只提交一次。
- 重复图片不会额外保留一条历史；应用会复用 canonical 记录身份，并在本机通过持久化删除/替换 intent 清理冗余或损坏的 backing。若操作中断，启动恢复会继续处理。
- 旧版本遗留的同像素图片记录会在启动恢复时合并；保留 canonical 记录身份、合并固定状态并采用最新复制时间。无引用且无法解码的图片 orphan 会进入 QuickStash 自有 Trash，不会重新显示为历史。
- 带 concealed 或 transient 标记的 pasteboard 内容会被跳过；`AutoGeneratedType` 不会单独导致其中的合法图片被忽略。
- 默认最多保留 100 条、最长 30 天；固定项目不会自动清理。
- 用户可以在设置中关闭记录或清理剪贴板历史。

## 文件与截图

- 拖入的文件会复制到 QuickStash 管理目录，而不是仅保存源路径引用。
- 截图渲染、PNG/JPEG 编码和保存均在本机完成。
- 复制截图是 QuickStash 内的直接用户操作：最终 PNG 会写入系统剪贴板并保存一份到本地历史，不受后台实时记录开关影响。
- 另存为操作只写入用户在系统 Save Panel 中选择的位置。

## 本地数据位置

默认数据目录：

```text
~/Library/Application Support/QuickStash/
```

主要内容：

- `items.json`：版本化历史 metadata。
- `Files`：QuickStash 管理的导入文件。
- `Images`：统一为 PNG 的新剪贴板图片、兼容保留的旧图片历史和 QuickStash 截图历史。
- `Importing`：尚未完成的导入暂存。
- `ImportManifests`：导入暂存、提交和回滚恢复所需的本地 intent。
- `DeletionManifests`：删除、冗余图片丢弃和损坏图片 backing 替换的本地 intent；记录受管理文件的原路径、目标 Trash 路径、源是否存在及其设备号/inode，并可在替换时包含新的本地记录 metadata。应用先写 intent，再变更文件或 metadata，成功后删除 manifest；中断后下次启动会先核对文件身份再继续或收敛状态，不会删除后来复用同一路径的新文件。
- `Trash`：应用自有的七天回收目录。

删除 QuickStash 历史或应用数据前，应先确认不再需要其中的托管副本。

## macOS 权限

### 屏幕与系统音频录制

截图功能使用 ScreenCaptureKit，需要 macOS 的“屏幕与系统音频录制”权限。权限在首次截图时由系统请求，授权后通常需要重新启动当前构建。

重新签名、更换 App 路径或重新构建后，macOS 可能把它识别为新的代码身份并再次请求权限。

### 不需要的权限

- 不请求辅助功能权限。
- 不声明虚构的 plist 截图权限键。
- 不需要通讯录、相机、麦克风或位置权限。

## 当前安全边界

- 当前工程未启用 App Sandbox。
- 开机自启动仅由用户在应用设置中控制。
- 正式分发版本仍需 Developer ID 签名、公证、staple 和干净机器验证。

安全问题请按 `SECURITY.md` 先确认仓库是否提供私密报告入口；若入口尚未启用，只公开请求安全联系方式，不要在公开 Issue 中附带漏洞细节、真实剪贴板、文件路径或截图内容。
