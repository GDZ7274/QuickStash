# QuickStash 安全策略

## 支持范围

安全修复以当前版本 `1.1.0 (4)` 和 `main` 分支为准。较早版本可能只在问题仍影响当前代码时处理。

## 报告方式

仓库公开后仍需由维护者启用并核验 GitHub **Private Vulnerability Reporting**；在仓库 Security 页面实际出现 **Report a vulnerability** 入口前，本文档不宣称该能力已经可用。若该入口已出现，请直接通过它提交私密报告。

若私密入口尚未启用，可在仓库 Issues 中创建标题为 `Security contact request` 的公开请求，只写受影响版本和“需要私密联系方式”，不要写漏洞原理、复现步骤、利用代码或任何用户数据。维护者建立私密渠道后再发送完整报告。这个公开请求流程可在仓库 Issue 列表中核验，但它本身不是漏洞报告渠道。

报告建议包含：

- 受影响版本和 macOS 版本；
- 可复现的最小步骤与实际影响；
- 仅使用虚构或脱敏数据的日志、截图或样例；
- 已知缓解方式。

不要在公开 Issue、Discussion 或 Pull Request 中发布未修复漏洞细节，也不要附带真实剪贴板内容、文件路径、截图、证书、访问令牌或其他个人信息。

## 响应原则

维护者会先确认收到报告，再评估影响、修复范围和披露时间。请在修复发布或双方约定披露前保持问题私密。

普通功能缺陷和不涉及安全的数据问题仍可使用公开 Issue，但必须先移除个人信息。
